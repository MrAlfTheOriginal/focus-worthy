[project]
name = "focus-worthy"
version = "1.0.0"
description = "Affiliate scraping platform"
requires-python = ">=3.10"
dependencies = [
    "flask",
    "flask-cors",
    "requests",
    "beautifulsoup4",
    "selenium",
    "pillow",
]

[tool.vercel]
entrypoint = "api:app"
